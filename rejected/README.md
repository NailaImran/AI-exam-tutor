# /rejected

Move approval request files here from /pending_approval/ to decline the action.

The orchestrator logs the rejection and Claude will not re-attempt the same action
unless a new request is created.
