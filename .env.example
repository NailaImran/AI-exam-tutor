# AI Exam Tutor - Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to git — it is already in .gitignore

# ============================================
# Safety Flags (set to true during development)
# ============================================
# DRY_RUN=true        → Log all actions, execute none (safe for testing)
# DEV_MODE=true       → Use sandbox/test accounts instead of real ones
DRY_RUN=false
DEV_MODE=false

# ============================================
# GitHub (Required for version control)
# ============================================
GITHUB_TOKEN=ghp_your_personal_access_token_here

# ============================================
# WhatsApp Business API (Phase 3)
# ============================================
# Get these from Meta Business Suite > WhatsApp > API Setup
WHATSAPP_PHONE_ID=your_whatsapp_phone_id
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token

# ============================================
# LinkedIn API (Phase 3)
# ============================================
# Get this from LinkedIn Developer Portal > Your App > Auth
LINKEDIN_ACCESS_TOKEN=your_linkedin_access_token

# ============================================
# Notes
# ============================================
# - Never commit .env to version control
# - WHATSAPP_PHONE_ID: Found in Meta Business Suite
# - WHATSAPP_ACCESS_TOKEN: Generate in WhatsApp API settings
# - LINKEDIN_ACCESS_TOKEN: OAuth 2.0 token with w_member_social scope
